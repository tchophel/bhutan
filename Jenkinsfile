pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Build application ...."
            }
        }

        stage('Testing') {
            steps {
                echo "Testing application..."
            }
        }
        stage('deploy') {
            steps {
                echo "deploy application..."
            }
        }

    }
}
