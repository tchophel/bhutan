pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Build application ...."
            }
        }

        stage('Testing') {
            steps {
                echo "Testing application..."
            }
        }
        stage('deploy') {
            ste {
                echo "deploy application..."
            }
        }

    }
}
