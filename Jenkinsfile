pipeline {
    agent any

    stages {

        stage('hello') {
            steps {
                echo "demo pull SCM"
            }
        }

        stage('Build') {
            steps {
                echo "Building application..."
            }
        }

    }
}
